const Discord = require("discord.js");
const fs = require("fs");

module.exports.run = async (client, message, args) => {

    console.log("dummy");
  }

  module.exports.help = {
    name: "dummy",
    aliases: ["none"],
    category: "none"
}
